class UnifiedTimelinePresentationFlags {
  const UnifiedTimelinePresentationFlags._();

  // PUTP placeholder flag. Wiring is intentionally deferred.
  static const bool unifiedTimelinePresentationLayer = false;
}
